// lib/data.js
import properties from '../properties.json';

// In Phase 2, this function will connect to Neon DB
export function getProperties() {
    // Simulate a database fetch and return the array
    return properties;
}